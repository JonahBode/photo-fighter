# Photo Fighter Assets

Place custom images, fonts, and other static assets in this folder.

Supported image formats for card uploads: JPEG, PNG, GIF, WebP.

The `card-image-upload` input in `index.html` handles reading images as
base64 data URLs — no server upload required.
