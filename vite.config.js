import { defineConfig } from 'vite';

export default defineConfig({
  // Required for GitHub Pages — assets resolve correctly at:
  // https://jonahbode.github.io/photo-fighter/
  base: '/photo-fighter/',
});
